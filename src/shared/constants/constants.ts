export const ROUTES = {
  HOME: "/",
  LOGIN: "/login",
} as const;

export const LINK_TARGETS = {
  BLANK: "_blank",
  SELF: "_self",
  PARENT: "_parent",
  TOP: "_top",
  FRAMENAME: "framename",
} as const;
